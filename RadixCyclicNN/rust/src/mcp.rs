//! The tools and the network over the Model Context Protocol (`radixnet/mcp.py`, `go/radixnet/mcp.go`).
//!
//! Not ported yet: a stub the port fills in.

use crate::cli::{not_ported, Ctx};

/// `radixnet mcp`.
pub fn cli(_ctx: &Ctx) -> Result<(), String> {
    Err(not_ported("mcp"))
}
